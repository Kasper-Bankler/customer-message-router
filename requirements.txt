# Lower bounds only, for now. Once the full stack installs and the demo runs end
# to end, these get replaced with exact versions from `pip freeze`, so the numbers
# quoted in the presentation are reproducible from a clean clone.
# Python 3.11+

# Typed I/O contracts between every agent
pydantic>=2.9

# LLM access. Ollama is the only client we ship; the Azure OpenAI path is
# described in llm.py but not installed, so the prototype has no cloud dependency.
ollama>=0.3

# Retrieval
sentence-transformers>=3.0
chromadb>=0.5
rank-bm25>=0.2

# Ingress guardrails. langdetect is weak on short strings; see ASSUMPTIONS.md #4.
langdetect>=1.0.9

# Data and evaluation. `datasets` was dropped: Banking77 on the Hub is a loader
# *script*, which datasets>=3 refuses to execute, so src/banking77.py fetches the
# same two CSVs over urllib instead. See DECISIONS.md.
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
matplotlib>=3.9

# Config
pyyaml>=6.0

# Demo UI
streamlit>=1.39

# Tests
pytest>=8.3
